/**
 * Página "Portafolio Digital" — ruta "/portfolio-digital".
 *
 * Por ahora es una página placeholder: muestra el componente `ComingSoon`
 * ("Próximamente") porque la sección de trabajos audiovisuales/edición
 * todavía no está lista. Cuando esté, se reemplaza ComingSoon por el
 * contenido real (videos, trabajos de edición, etc.).
 */

import ComingSoon from "@/app/components/ComingSoon";

export default function PortfolioDigitalPage() {
  return <ComingSoon />;
}
